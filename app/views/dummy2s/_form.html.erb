<%= form_with(model: dummy2, local: true) do |form| %>
  <% if dummy2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dummy2.errors.count, "error") %> prohibited this dummy2 from being saved:</h2>

      <ul>
      <% dummy2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
