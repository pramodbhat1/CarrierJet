 <% if !user_signed_in? %>
  <%= link_to 'Sign in', new_user_session_path %> <br>
  <%= link_to 'Sign up', new_user_registration_path %> 
   <% end %>
   <% if user_signed_in? %>
   <%= link_to('Logout', destroy_user_session_path, :method => :delete)  %>
   <% end %>

<%= link_to 'New Newloginpage', new_newloginpage_path %>
